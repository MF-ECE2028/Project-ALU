\documentclass[12pt,letterpaper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{svg}
\usepackage{hyperref}
\usepackage{tikz}
\usepackage{caption}
\usepackage{array}
\usepackage{lscape}
\usepackage{pgfplots}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{minted}
\usemintedstyle{monokai}
\pgfplotsset{width=17cm, compat=1.18}
\usetikzlibrary{angles, quotes}
\usepackage[left=2cm,right=2cm,top=2cm,bottom=2cm]{geometry}
\author{Nolan Kessler (kesslnol@oregonstate.edu) \\ Maxwell Fabian (fabianma@oregonstate.edu)\\ John O'Connor (oconnoj5@oregonstate.edu)}
\title{ECE 204 Design Project \\
       \normalsize \textit{8 Bit ALU}}
       
\begin{document}
%\definecolor{commentgreen}{rgb}{0,0.7,0}
\definecolor{bggray}{rgb}{0.2,0.2,0.2}
%\definecolor{basegray}{rgb}{0.8,0.8,0.8}
%\definecolor{keywordblue}{rgb}{0,0.5,1}
%\definecolor{identifierblue}{rgb}{0,1,1}
%\definecolor{datatypegreen}{rgb}{0,1,0.5}
\definecolor{black}{rgb}{0,0,0}
\definecolor{white}{rgb}{1,1,1}
%\definecolor{stringorange}{rgb}{1, 0.4, 0.4}

\lstdefinestyle{output}{
    backgroundcolor=\color{black},   
    numberstyle=\tiny \color{black},
    basicstyle=\ttfamily \footnotesize \color{white},
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                                     
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2
}

\lstset {style = output}


\maketitle

\begin{center}
    Demonstration Video: \\
    \urlstyle{rm}
    \url{https://media.oregonstate.edu/media/t/1_ci3vfnca}
\end{center}

\begin{figure*}[h!]
    \centering
    \includegraphics*[scale=0.45]{./Diagrams/full_alu_schematic.png}
\end{figure*}

\pagebreak

\tableofcontents

\pagebreak

\section{8 Bit ALU Functional Description}

This 8 bit ALU accepts two 8 bit operands A and B, along with a 3 bit opcode (OP). 
The state of the ALU is controlled by an active high enable pin (EN), and can be 
reset using the active low reset pin (RST\_N). As this ALU supports sequential 
operations which may take more than one clock cycle, the CLK input must be connected 
to a clock signal. The ALU provides one 8 bit result output (Y) an overflow flag (OVER)
and a done flag (DONE). A table of the ALU's supported operations is given in table 
\ref{operations_table}. A diagram of the inputs and outputs is shown in figure \ref{alu_io_diagram}.
A description of the function of each bus/pin is given in table \ref{alu_io_table}.


\begin{table}[h]
\centering
\setlength{\extrarowheight}{4pt}
\begin{tabular}{|l|l|l|l|p{5cm}|}
\hline
\textbf{Operation} & \textbf{Opcode} & \textbf{Clock Cycles} & \textbf{Overflow Condition} & \textbf{Description}                                       \\ \hline \hline
A pass             & 0x0             & 1                     & N/A                         & Passes A to Y unmodified                                   \\ \hline
B pass             & 0x1             & 1                     & N/A                         & Passes B to Y unmodified                                   \\ \hline
Bitwise AND        & 0x2             & 1                     & N/A                         & Y is bitwise AND combination of operands A and B           \\ \hline
Bitwise OR         & 0x3             & 1                     & N/A                         & Y is bitwise OR combination of operands A and B            \\ \hline
Bitwise XOR        & 0x4             & 1                     & N/A                         & Y is bitwise XOR combination of operands A and B           \\ \hline
Addition           & 0x5             & 1                     & $A + B > 255$               & Y is the sum of operands A and B                           \\ \hline
Subtraction        & 0x6             & 1                     & $A + (\bar{B} + 1) > 255$   & Y is the sum of A and the 2's complement inverse of B \\ \hline
Multiplication     & 0x7             & 9                     & $A * B > 255$               & Y is the product of A and B                                \\ \hline
\end{tabular}
\caption{Table of supported operations}
\label{operations_table}
\end{table}

\begin{figure}[h]
    \centering
    \includegraphics[scale=0.5]{./Diagrams/alu_black_box_diagram.png}
    \caption{ALU Inputs and Outputs}
    \label{alu_io_diagram}
\end{figure}

\begin{table}[h]
\setlength{\extrarowheight}{4pt}
\centering
\begin{tabular}{lll}
\hline
\multicolumn{1}{|l|}{\textbf{Bus / Pin}} & \multicolumn{1}{l|}{\textbf{Width (Bits)}} & \multicolumn{1}{p{10cm}|}{\textbf{Function}}                                                                                                                    \\ \hline
\textbf{Input}                           &                                            &                                                                                                                                                \\ \hline
\multicolumn{1}{|l|}{A}                  & \multicolumn{1}{l|}{8}                     & \multicolumn{1}{p{10cm}|}{First 8-Bit operand - input to mathematical or logical operation.}                                                                    \\ \hline
\multicolumn{1}{|l|}{B}                  & \multicolumn{1}{l|}{8}                     & \multicolumn{1}{p{10cm}|}{Second 8-Bit operand - input to mathematical or logical operation.}                                                                   \\ \hline
\multicolumn{1}{|l|}{OP}                 & \multicolumn{1}{l|}{3}                     & \multicolumn{1}{p{10cm}|}{Opcode - selects which operation to perform.}                                                                                         \\ \hline
\multicolumn{1}{|l|}{RST\_N}             & \multicolumn{1}{l|}{1}                     & \multicolumn{1}{p{10cm}|}{Active low asynchronous reset - puts the ALU into initial ready state.}                                                               \\ \hline
\multicolumn{1}{|l|}{EN}                 & \multicolumn{1}{l|}{1}                     & \multicolumn{1}{p{10cm}|}{Enable - starts ALU when high if in ready state. Must be high for ALU to output data.}                                                             \\ \hline
\textbf{Output}                          &                                            &                                                                                                                                                      \\ \hline
\multicolumn{1}{|l|}{Y}                  & \multicolumn{1}{l|}{8}                     & \multicolumn{1}{p{10cm}|}{8 Bit result - output of the ALU operation}                                                                                           \\ \hline
\multicolumn{1}{|l|}{OVER}               & \multicolumn{1}{l|}{1}                     & \multicolumn{1}{p{10cm}|}{Overflow flag - high value generally indicates result cannot be represented in bits of Y. Exact meaning can vary between operations.} \\ \hline
\multicolumn{1}{|l|}{DONE}               & \multicolumn{1}{l|}{1}                     & \multicolumn{1}{p{10cm}|}{Is low while operation is in progress or ALU is ready, high when operation is completed.}                                                             \\ \hline
\end{tabular}
\caption{ALU IO Functional Descriptions}
\label{alu_io_table}
\end{table}

\subsection{ALU Operation}
Before beginning an operation the ALU must be reset by setting RST\_N low, after reset occurs the
ALU is considered to be in the "ready" state. EN must be low for all rising edges of CLK while the
ALU is in the ready state. Once the inputs on A, B, and OP are stable, EN can be set
high to begin ALU operation. The rising edge of EN must occur when CLK is low. EN must remain high until after the next falling edge of CLK. At this
point the ALU enters the "running" state. From this point on the value of EN can be changed without 
effecting the ALU (or the result when the ALU is done). The ALU will always output all zeros when EN
is low. When DONE goes high the ALU is in the "done" state. The operation is finished, and results 
can be read from Y, and OVER. A timing diagram of a normal ALU operation is shown in figure \ref{alu_timing}.

\begin{figure}
    \centering
    \includegraphics[scale=0.5]{./Diagrams/timing_diagram.png}
    \caption{ALU Operation Timing Diagram}
    \label{alu_timing}
\end{figure}

\pagebreak

\section{Design and Implementation}
This ALU is designed to utilize modular operations, to allow the design to allow for easy testing during the design process,
and potential rapid addition of new functions in the future. These modular operations are then linked together with
integrated together with structures to allow for the selection of operations, forming the complete ALU.

\subsection{Operation Modules}
Each operation module accepts the inputs, and supplies the outputs shown in table \ref{operation_io_table}.
All outputs of the modules are routed through tri-state buffers - the outputs of an operation are floating
unless its enable pin is high.

There are two primary types of operation modules utilized in this ALU, combinational logic operations,
and sequential logic operations. For combinational logic operations, the CLK and RST\_N inputs are not
needed, and DONE is always high when the module is enabled. A diagram of such an operation module is shown
in figure \ref{cl_module_diagram}. Sequential logic operations utilize all inputs to perform operations
that require more than one clock cycle. The only such operation in this ALU is multiplication. A diagram of
this type of module is shown in \ref{seq_module_diagram}. 


\begin{figure}[h!]
    \centering
    \includegraphics[scale=0.5]{./Diagrams/cl_diagram.png}
    \caption{Schematic of a Combinational Operation Module}
    \label{cl_module_diagram}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[scale=0.5]{./Diagrams/sequential_logic_diagram.png}
    \caption{Schematic of a Sequential Operation Module}
    \label{seq_module_diagram}
\end{figure}

\begin{table}[h!]
    \setlength{\extrarowheight}{4pt}
    \centering
    \begin{tabular}{lll}
    \hline
    \multicolumn{1}{|l|}{\textbf{Bus / Pin}} & \multicolumn{1}{l|}{\textbf{Width (Bits)}} & \multicolumn{1}{p{10cm}|}{\textbf{Function}}                                                                                                                    \\ \hline
    \textbf{Input}                           &                                            &                                                                                                                                                                 \\ \hline
    \multicolumn{1}{|l|}{A}                  & \multicolumn{1}{l|}{8}                     & \multicolumn{1}{p{10cm}|}{First 8-Bit operand - input to operation.}                                                                                            \\ \hline
    \multicolumn{1}{|l|}{B}                  & \multicolumn{1}{l|}{8}                     & \multicolumn{1}{p{10cm}|}{Second 8-Bit operand - input to operation.}                                                                                           \\ \hline
    \multicolumn{1}{|l|}{RST\_N}             & \multicolumn{1}{l|}{1}                     & \multicolumn{1}{p{10cm}|}{Active low asynchronous reset - resets the module to inial ready state.}                                                              \\ \hline
    \multicolumn{1}{|l|}{EN}                 & \multicolumn{1}{l|}{1}                     & \multicolumn{1}{p{10cm}|}{Enable - enables the module. The module will only output data when this pin is high}                                                  \\ \hline
    \textbf{Output}                          &                                            &                                                                                                                                                                 \\ \hline
    \multicolumn{1}{|l|}{Y}                  & \multicolumn{1}{l|}{8}                     & \multicolumn{1}{p{10cm}|}{8 Bit result - output of the operation}                                                                                               \\ \hline
    \multicolumn{1}{|l|}{OVER}               & \multicolumn{1}{l|}{1}                     & \multicolumn{1}{p{10cm}|}{Overflow flag - high value generally indicates result cannot be represented in bits of Y. Exact meaning can vary between operations.} \\ \hline
    \multicolumn{1}{|l|}{DONE}               & \multicolumn{1}{l|}{1}                     & \multicolumn{1}{p{10cm}|}{Is low while operation is in progress or is ready, high when operation is completed.}                                             \\ \hline
    \end{tabular}
    \caption{Operation Module IO Descriptions}
    \label{operation_io_table}
\end{table}

The following sections will include a brief description of the implementation of each ALU operation, and a listing of its
SystemVerilog code.

\subsubsection{A Pass}
A Passthrough passes the 8-bit operand A to the output Y without modification. In the case enable is low, all outputs float to Z.
\subsubsection*{Listing: a\_pass.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/a_pass.sv"}


\subsubsection{B Pass}
B Passthrough passes the 8-bit operand B to the output Y without modification. In the case enable is low, all outputs float to Z.
\subsubsection*{Listing: b\_pass.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/b_pass.sv"}


\subsubsection{Bitwise AND}
Bitwise AND performs the logical AND operation on each corresponding bit of the operands A and B, setting the result of this to Y.
A bit in Y is 1 if and only if the corresponding bits and A and B are booth 1. When enable is low, all outputs float.
\subsubsection*{Listing: bw\_and.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/bw_and.sv"}


\subsubsection{Bitwise OR}
Bitwise OR performs the logical OR operation on each corresponding bit of the operands A and B, setting the result of this to Y. 
A bit in Y is 1 if either or both corresponding bits in A and B are 1. When enable is low, all outputs float.
\subsubsection*{Listing: bw\_or.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/bw_or.sv"}


\subsubsection{Bitwise XOR}
Bitwise XOR performs the logical XOR operation on each corresponding bit of the operands A and B, setting the result of this to Y.
A bit in Y is 1 if either the of the corresponding bits in A or B is 1 but both. When enable is low, all outputs float.
\subsubsection*{Listing: bw\_xor.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/bw_xor.sv"}


\subsubsection{Addition}
Addition performs unsigned addition on two 8-bit operands A and B, setting the result of this to Y. In the case where the result
of A + B exceeds 255, overflow will be set to 1, representing the most significant bit. When enable is low, all outputs float.
\subsubsection*{Listing: addition.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/addition.sv"}


\subsubsection{Subtraction}
Subtraction performs unsigned subtraction on two 8-bit operands A and B, setting the result of this to Y. This implementation uses 
the two's compliment of B and computes subtraction by adding A to the two's compliment of B. When enable is low, all outputs float.
\subsubsection*{Listing: subtraction.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/subtraction.sv"}


\subsubsection{Multiplication}
Multiplication performs a binary sequential multiplication by running for 9 clock cycles for 8 cycles the value for A has a logical left shift applied and is added to the result register if the equivalent bit in B is high. This functions similarly to writing out long multiplication by hand. The schematic is shown in Figure \ref{fig:MultSchematic}. 
	\begin{figure}[h!]
		\centering
		\includegraphics[scale=0.3]{./Diagrams/mult_diagram.png}
		\caption{Schematic of the Multiplication Module}
		\label{fig:MultSchematic}
	\end{figure}
This can also be modeled as a state machine with 3 states, an initial state, multiplication state, and an finished state. Where the initial state sets the registers to their reset state, sending to the multiplication state. The multiplication state shifts the multiplicand and adds it to the result when conditions are met, and re-enters the multiplication state until it has completed this state 8 times, exiting to the final state. The final state sets the done flag high and sets the output and overflow accordingly. This can be visualized in Figure \ref{fig:MultStateMach}.
	\begin{figure}[!h]
		\centering
		\includegraphics[scale=0.7]{./Diagrams/mult_state.png}
		\caption{State Machine Diagram for the Multiplication Module}
		\label{fig:MultStateMach}
	\end{figure} 
\subsubsection*{Listing: mult.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/mult.sv"}


\subsection{ALU Utility Modules}
Three SystemVerilog modules were developed to be used within ALU operations or the ALU itself. These include a variable width
asynchronously resettable register with enable, a 3 bit multiplexer with enable, and a variable width counter. The Implementation
details and SystemVerilog listings for these modules are provided in the subsequent sections.

\subsubsection{Opcode Decoder}
The decoder takes a 3-bit opcode input and converts it to an 8-bit, one-hot signal used to enable a given operation module. 
When enable is low, all outputs are set to 0.
\subsubsection*{Listing: multiplexer\_3\_en.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/utility_modules/decoder.sv"}

\subsubsection{Variable Width Register}
The variable width register is a configurable register, with a default width of 1, used to store the value of the in signal. If enable is high, 
the input "in" is copied to the output on the rising clock edge. Then, when enable is low, the register holds the value of out. If reset goes 
low, the output is immediately set to 0.
\subsubsection*{Listing: decoder.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/utility_modules/register_en_rstn.sv"}

\subsubsection{Counter}
The counter is an N width counting module with a default width of 4 bits. On the rising clock edge, the counter increments itself by the
value of addBy. If reset is high, the counter resets the count to 0.
\subsubsection*{Listing: counter.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/utility_modules/counter.sv"}

\subsection{ALU Implementation}
A schematic diagram of the full ALU is shown in figure \ref{full_alu_schematic}. Four registers are used to ensure that the inputs to the ALU remain
consistent throughout the entire operation. $R_a$, $R_b$, and $R_op$ will store the inputs to the ALU on the first rising clock edge that occurs while
EN is high. The output of the locking register $R_lk$ becomes high on the next clock falling edge, disabling further updates to A, B, and OP, and
enabling the decoder, which will select the requested function. At this point the value of EN can be changed without impacting the ALU operation.

\begin{figure}[h!]
    \centering
    \includegraphics[scale=0.6]{./Diagrams/full_alu_schematic.drawio.png}
    \caption{Schematic of Full ALU}
    \label{full_alu_schematic}
\end{figure}

\section{Design Verification}
Design verification is done using SystemVerilog test benches. Each operation module has it own test bench to test expected values compared to simulated.
The ALU itself its own test bench which ensures the ALU design and utility modules function as expected.

\subsection{Operation Test Benches}
Each operation module has a test bench. All test benches with the exception of the multiplication test bench test all possible combinations of operands as well as testing the case where the enable signal is low.

\subsubsection{A Pass}
\subsubsection*{Listing: a\_pass.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/testbenches/a_pass.tb.sv"}


\subsubsection{B Pass}
\subsubsection*{Listing: b\_pass.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/testbenches/b_pass.tb.sv"}


\subsubsection{Bitwise AND}
\subsubsection*{Listing: and.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/testbenches/and.tb.sv"}


\subsubsection{Bitwise OR}
\subsubsection*{Listing: bw\_or.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/testbenches/bw_or.tb.sv"}


\subsubsection{Bitwise XOR}
\subsubsection*{Listing: bw\_xor.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/testbenches/bw_xor.tb.sv"}


\subsubsection{Addition}
\subsubsection*{Listing: addition.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/testbenches/addition.tb.sv"}


\subsubsection{Subtraction}
\subsubsection*{Listing: subtraction.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/testbenches/subtraction.tb.sv"}

\subsubsection{Multiplication}
\subsubsection*{Listing: mult.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/functions/testbenches/mult.tb.sv"}


\subsection{ALU Test Bench}
The ALU test bench tests the ALU design as well as the functionality of the utility modules. It iterates through each operation by testing one opcode
at a time, resetting between tests.
\subsubsection*{Listing: alu.tb.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/alu.tb.sv"}

\subsection{Simulation Testing}
Simulation can be done using ModelSim. To accompany this, we can run a do file which runs all test benches.
\subsubsection*{Listing: ALUSim.do}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALUSim.do"}

\subsubsection*{Do File Output}
\lstinputlisting{"do_file_output.txt"}

\subsubsection*{ALU Test Bench Waveform}
We can analyze the ALU test bench's waveform using the waveform
\begin{figure}[h!]
    \centering
    \includegraphics[scale=0.5]{./Diagrams/alu_waveform.png}
    \caption{Waveform Generated by ALU Test Bench}
    \label{ALU Test Bench Waveform}
\end{figure}

\section{Hardware Validation}
Hardware validation requires a couple additional modules for the ALU to work effectively on the FPGA.
To achieve two 8-bit inputs, we need to be able to input operand A, then have a register hold that value
while we input operand B, then a register to hold B while we input the opcode. This is a achieved using a
four state FSM, where the state is used to control a four-bit bus demultiplexer. When the final user input
state is finished, the ALU is enabled and the state counter is disabled on the next clock falling edge. The
display output is switched from the switch input (Num) to the alu output Y, using a one bit bus multiplexer.
The FSM will remain in this state until the system is reset. A schematic and state transition diagram are
shown in figures \ref{alu_hardware_schematic} and \ref{alu_fsm} respectively.
Listings of the SystemVerilog implementations of this system and its components are included in the next section.

\begin{figure}[h!]
    \centering
    \includegraphics[scale=0.35]{./Diagrams/alu_demo_schematic.png}
    \caption{ALU Hardware Implementation Schematic}
    \label{alu_hardware_schematic}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[scale=0.5]{./Diagrams/demo_state_machiene.png}
    \caption{ALU Hardware Implementation State Machine}
    \label{alu_fsm}
\end{figure}

\subsection{Hardware Implementation HDL Listings}

\subsubsection*{Listing: alu\_test.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/hardware_validation/alu_test.sv"}

\subsubsection*{Listing: bus\_demultiplexer\_2.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/hardware_validation/bus_demultiplexer_2.sv"}

\subsubsection*{Listing: abus\_multiplexer\_1.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/hardware_validation/bus_multiplexer_1.sv"}

\subsubsection*{Listing: SevenSegmentDecode.sv}
\inputminted[bgcolor = bggray, linenos, fontsize = \footnotesize, breaklines]{sv}{"../ALU_SystemVerilog/hardware_validation/SevenSegmentDecode.sv"}


\pagebreak
\section{Reflection on Design Process}
Implementing the project overall when well. Implementing each individual combinational modules
and getting them working in the larger ALU module was quick and relatively simple. However
we ran into some complications when attempting to implement a more complicated sequential
module. This was the multiplication module. Since it required multiple clock cycles to perform,
the module needed to have proper timing and a comparator to track the current clock cycle the
system is on and run the system accordingly. We ran into a few issues with the done and overflow
registers being out of sync causing the module to fail to properly output the overflow flag. These
smaller difficulties were more time consuming than difficult as they were small errors that required 
looking over the HDL script and RTL Viewer to identify where the timing issues would occur.

\end{document}
